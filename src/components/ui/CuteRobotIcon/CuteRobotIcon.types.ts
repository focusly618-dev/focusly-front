export interface CuteRobotIconProps {
  size?: number;
  /** 'full' = animated mascot SVG. 'mini' = compact 1-color version for bubbles */
  variant?: 'full' | 'mini';
  primaryColor?: string;
  eyeColor?: string;
}

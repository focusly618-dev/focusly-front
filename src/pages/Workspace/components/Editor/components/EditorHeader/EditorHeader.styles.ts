import {
  EditorHeader as StyledEditorHeader,
  BackButton as StyledBackButton
} from '../../WorkspaceEditor.styles';

export const EditorHeader = StyledEditorHeader;
export const BackButton = StyledBackButton;

/**
 * Feature Flags Configuration
 * Centralized place to toggle application features.
 */
export const FEATURE_FLAGS = {
  LIMIT_AI_CONVERSATIONS: true, // Limits AI conversations to 4 when active
  // Add other flags here in the future
};
